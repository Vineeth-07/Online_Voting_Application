<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Results</title>
</head>
<body>
    <% if(messages && messages.error && messages.error.length > 0)  { %>
        <% for(var i=0; i<messages.error.length; i++) { %>
          <p class="bg-red-200 my-3 list-none rounded px-4 py-2"><%= messages.error[i] %></p>
        <% } %>
    <% } %> 
    <div class="grid grid-cols-12 items-center mt-10">
        <div class="flex flex-col gap-6 col-start-2 col-span-6">
            <span class="text-4xl font-medium">Election has not ended yet!</span>
            <span class="text-2xl font-medium">Please come back for results after the election ends.</span>
        </div>
        <div class="hidden md:block col-span-4 col-start-8">
            <img src="/images/thankyou.png" alt="hero image">
        </div>
    </div>
</div>
</body>
</html>